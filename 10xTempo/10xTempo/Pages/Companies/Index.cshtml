@page
@model _10xTempo.Pages.Companies.IndexModel
@{
    ViewData["Title"] = "Twoje firmy";
}

<section class="card stack">
    <div class="page-header">
        <div>
            <h2>Firmy</h2>
            <p class="muted">Zarządzaj firmami i rolami związanymi z Twoim kontem.</p>
        </div>
        <div class="actions">
            <a class="button primary" asp-page="Create">
                <span aria-hidden="true">➕</span> Nowa firma
            </a>
            <a class="button ghost" asp-page="Join">
                <span aria-hidden="true">🔗</span> Dołącz po GUID
            </a>
        </div>
    </div>

    @if (!Model.Companies.Any())
    {
        <p>Nie należysz jeszcze do żadnej firmy. Utwórz firmę lub dołącz po GUID.</p>
    }
    else
    {
        <div class="table-wrapper">
            <table class="table">
                <thead>
                <tr>
                    <th>Nazwa</th>
                    <th>GUID</th>
                    <th>Rola</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var company in Model.Companies)
                {
                    <tr>
                        <td>@company.Name</td>
                        <td><code>@company.Id</code></td>
                        <td>@company.Role</td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    }
</section>
